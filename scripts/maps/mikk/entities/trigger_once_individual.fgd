@BaseClass = Base
[
	targetname(target_source) : "Name"
	target(target_destination) : "Target"
]

@BaseClass base(Base) color(164 0 255) = trigger_once_individual : "Trigger individually per player"[]

@PointClass base(Base) color(164 0 255) = trigger_once_individual : "Trigger individually per player"
[
	vuser1(string) : "Custom Min Hull Size (X Y Z)"
	vuser2(string) : "Custom Max Hull Size (X Y Z)"
]