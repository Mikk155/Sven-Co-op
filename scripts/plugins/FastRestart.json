{
    // Reload the map after this many seconds
    "reload_time": 1.5,

    // Whatever to wait if there are a medic npc nearby (scientist and medic grunt can revive players)
    "wait_medic": true,

    // Radius in units (sphere) to look up for medic monsters
    "medic_radius": 1024,

    // Classname of medic entities to look up for
    "medic_entities":
    [
        "monster_scientist",
        "monster_human_medic_ally"
    ],

    // Should this json file be parsed every map change?
    "reload": false,

    // List of maps that this plugin should be disabled.
    // Supports the wildcard "*" as suffix and prefix
    "map_blacklist":
    [
        "bts_rc",
        "of*"
    ]
}
