{
    // -1 = default
    // Greater = faster it consumes
    // 0 = infinite
    // Less = slowest it consumes
    "speed consume": 10,

    // -1 = default
    // Recharges this ammount of battery every time the player thinks
    // 0 = do not recharge
    "speed recharge": -1
}